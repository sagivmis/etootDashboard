export type PrevStats = {
  locationTag: string
  maxTemp: string
  minTemp: string
  email: string
  phone: string
}

export type ChangeStats = {
  locationTag: boolean
  maxTemp: boolean
  minTemp: boolean
  email: boolean
  phone: boolean
}
