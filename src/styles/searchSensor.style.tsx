import { StyleSheet } from 'react-native';
import colors from '../config/colors'

const searchSensorStyle = StyleSheet.create({
    searchSensorContainer: {
        margin: 10
    },
    customSearchInputContainer: {
        borderColor: 'black',
        borderWidth: 1,
        width: '95%'
    }
})

export default searchSensorStyle;