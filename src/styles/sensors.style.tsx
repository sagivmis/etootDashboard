import { StyleSheet } from 'react-native';
import colors from '../config/colors'

const sensorsStyle = StyleSheet.create({
    sensorsContainer: {
        width: '95%',
        flex: 2,
        flexDirection: 'column'
    }
})

export default sensorsStyle;